# Conversions (numerial systems)

Conversions is a library contains a set of functions designed to Convert the number to its equivalents in other numerial systems (binary system, octal system, decimal system, hexadecimal system). This implementation doesn't give the best performance but it does save the memory being used.
