# Conversions (numerial systems)

Conversions is a library contains a set of functions designed to Convert the number to its equivalents in other numerial systems (binary system, octal system, decimal system, hexadecimal system).
